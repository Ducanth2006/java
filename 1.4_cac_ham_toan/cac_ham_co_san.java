public class cac_ham_co_san {
    public static void main (String[] args){
        // The abs function returns the absolute value of a number.
        // the pow function returns a double value and sqrt too 
        // 3!=3.0
        //ceil floor round : The round function rounds(lam tron) a number to the nearest integer, which depends on its fractional(thap phan) part.

        int a=-109,b=9,c=2,d=2;
        float f=23.3947F;
        System.out.print(Math.abs(a)+"|"+Math.sqrt(b)+"|"+Math.pow(c,d)+"\n");
        System.out.println(Math.max(b,c));
        System.out.println(Math.floor(f));// it will round down 
        System.out.println(Math.round(f));// it will depend on fractional(thap phan) to round
        

    }
}
